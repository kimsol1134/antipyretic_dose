# e약은요 API Key
# https://www.data.go.kr/에서 발급받은 API 키
EASY_DRUG_API_KEY=your_easy_drug_api_key_here

# Google Service Account JSON (JSON 전체를 한 줄로)
# Google Cloud Console에서 Service Account를 생성하고 JSON 키를 다운로드한 후,
# 전체 JSON 내용을 문자열로 복사하여 붙여넣으세요.
#
# 설정 방법:
# 1. Google Cloud Console (https://console.cloud.google.com/) 접속
# 2. 프로젝트 생성 또는 선택
# 3. "APIs & Services" > "Library"에서 "Indexing API" 검색 및 활성화
# 4. "APIs & Services" > "Credentials"에서 "Create Credentials" > "Service Account" 선택
# 5. Service Account 생성 후 "Keys" 탭에서 "Add Key" > "Create new key" > "JSON" 선택
# 6. 다운로드된 JSON 파일의 내용을 아래에 붙여넣기 (줄바꿈 제거하거나 그대로 유지)
#
# 예시:
# GOOGLE_SERVICE_ACCOUNT_KEY={"type":"service_account","project_id":"your-project-id",...}
#
# 또는 여러 줄로:
# GOOGLE_SERVICE_ACCOUNT_KEY='{
#   "type": "service_account",
#   "project_id": "your-project-id",
#   "private_key_id": "...",
#   "private_key": "-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n",
#   "client_email": "your-service-account@your-project-id.iam.gserviceaccount.com",
#   ...
# }'
GOOGLE_SERVICE_ACCOUNT_KEY=
